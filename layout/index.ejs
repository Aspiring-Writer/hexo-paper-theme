<header class="text-center">
  <h1>
    <% if (is_home) { %>Home<% } else { %> <%= page.title %> <% } %>
  </h1>
  <% if (page.subtitle) { %>
  <h3><%= page.subtitle %></h3>
  <% } %>
</header>

<section>
  <%- partial('_partial/posts-list.ejs') %>
</section>
<%- partial('_partial/pagination') %>
